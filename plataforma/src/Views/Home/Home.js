import React from "react";

const HomeView = () => {
    return (
        <section className="hero">
            <nav>
                <h2>Home</h2> 
            </nav>
        </section>
    );
};

export default HomeView;
